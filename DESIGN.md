# 倌榔小程序界面约定

消费者通常手持手机在门店或日常环境下兑奖，需要快速辨认品牌、输入兑换码和确认领取状态。

## Existing Theme

沿用 `uni.scss` 中的设计变量。包装深蓝为品牌主色，棕金用于兑奖强调，内容位于冷瓷白底色上。当前功能增补不重做整体视觉。

## Typography and Layout

使用现有中文系统字体、rpx 尺寸和 `$bl-pad` 页面边距。正文使用 `$bl-fs-body`，次要操作使用 `$bl-fs-sm`。保持已有卡片和按钮圆角。

## Interaction

分享使用微信原生 `button open-type="share"`。首页的分享按钮位于兑奖说明下方，采用次级蓝色样式，触控高度至少 88rpx，文字标签明确。使用既有 `bl-icon` 分享图标。点击态使用颜色变化，不新增装饰动画。

## Privacy

转发卡片采用固定的公开活动标题、首页路径和公开品牌图片，不使用个人凭证页面截图。微信特有入口仅在微信小程序构建中显示。
